import React, { createContext } from "react";

export const FontFamilyContext = createContext('sans-serif');

export const FontOptions = ["BodoniMada", "NunitoSans", "Quicksand", "RobotoSerif", "serif", "sans-serif"];
